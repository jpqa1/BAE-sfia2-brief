   all:
      children:
        docker:
          hosts:
            10.0.1.51: #docker
          vars:
            ansible_ssh_private_key_file: ~/.ssh/ssh-aws-pc
            ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
            ansible_user: ubuntu
        jenkins:
          hosts:
            10.0.1.50: #Jenkins
          vars:
            ansible_ssh_private_key_file: ~/.ssh/ssh-aws-pc
            ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
            ansible_user: ubuntu
        test:
          hosts:
            10.0.1.52: #test
          vars:
            ansible_ssh_private_key_file: ~/.ssh/ssh-aws-pc
            ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
            ansible_user: ubuntu
